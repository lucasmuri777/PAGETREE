export type User = {
    name: string;
    email: string;
    image: string;
}
export type UserStripe = {
    stripeId: string
    id: number;
    name: string;
    email: string;
    image: string;
}